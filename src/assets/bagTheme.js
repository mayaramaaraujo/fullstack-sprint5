import { createTheme } from "@material-ui/core/styles";

const bagTheme = createTheme({
  palette: {
    primary: {
      main: "#008844",
    },
    secondary: {
      main: "#D0403A",
    },
  },
});

export default bagTheme;
