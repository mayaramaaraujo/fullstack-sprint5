import { createTheme } from '@material-ui/core/styles';

const bagTheme = createTheme({
  status: {
    neutral: "#000"
  },
  palette: {
    primary: {
      main: '#008844',
    },
    secondary: {
      main: '#D0403A',
    }
  },
});

export default bagTheme;